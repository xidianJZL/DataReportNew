开发一个用大模型根据数据表格生成复盘报告的网页工具，功能如下：
1. 用户上传 Excel 表格和分析需求后，大模型会根据任务撰写 Python 代码、调用 run_code 工具处理数据、分析数据，最终生成交付一份完整的可视化（基于 ECharts）分析报告。
2. 要让用户可以通过提供base_url,model_name和API_KEY 来配置大模型
3. 分析过程应该是计划性的，模型根据任务和数据自主多轮交互，而不是一次性完成。LLM 作为项目的独立决策者，负责制定计划、决定调用哪个工具、根据工具返回决定进行下一步还是结束，即 “LLM makes the loop、LLM in the loop、LLM ends the loop”，示例代码见`loop_demo.py`
4. run_code 使用 Python 的动态执行，示例代码见`run_code_demo.py`。代码仅用于本地测试，风险是可控的。
5. 前端通过流式协议接收后端推送的结构化分片返回（LLM 的 API 支持流式输出），但需要把不同类型的内容路由到不同的 UI 组件分开显示。因此，需要对 LLM 的输出格式进行协议化约束，使用显式标签将推理内容、工具决策和面向用户的回答在文本流中层次化标记。后端在流读取过程中实现轻量级协议解析器，将长文本切分为结构化事件，再通过前端订阅的事件流通道分发给对应 UI 模块。
6. 前端应支持 ECharts 图表、Markdown、Mermaid 等常用语法的渲染
7. 使用 shadcn/ui 构建现代化美观界面，风格简约高级，采用柔和阴影、舒适间距、优雅圆角与细腻过渡动画，主色调柔和统一，界面层次清晰、呼吸感强，视觉干净不杂乱，支持明暗主题自适应，整体呈现专业、高端、易用的 SaaS 产品质感。
8. 注意：示例代码仅供参考，真实项目应重构提示词、可用工具、状态管理、错误恢复、日志持久化等逻辑
9.启动项目要很方便，项目要运行在虚拟环境避免污染我的本地环境，给我写一个很方便启动项目和关闭项目的脚本

UI:
UI风格要严格按照：https://github.com/VoltAgent/awesome-design-md/blob/main/design-md/cohere/DESIGN.md